import React from 'react';
import ExpenseChart from '../ExpenseChart';

const Dashboard = () => {
    return (
        <div>
            <h2>Dashboard</h2>
            <ExpenseChart />
        </div>
    );
};

export default Dashboard;
